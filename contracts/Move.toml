[package]
name = "sonar"
version = "0.1.0"
edition = "2024.beta"
license = "Apache-2.0"
authors = ["SONAR Protocol <team@sonar.xyz>"]

[dependencies]
# IMPORTANT: Framework pinned to testnet-v1.43.0 for testnet deployment compatibility
# MUST use matching Sui CLI v1.43.0 for builds and tests
# Install with: cargo install --locked --git https://github.com/MystenLabs/sui.git --tag testnet-v1.43.0 sui --force
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "testnet-v1.43.0" }
Walrus = { local = "./dependencies/walrus" }
WAL = { local = "./dependencies/wal" }

[addresses]
sonar = "0x0"
walrus = "0x795ddbc26b8cfff2551f45e198b87fc19473f2df50f995376b924ac80e56f88b"
# Note: walrus address updated to match current testnet deployment
# Deployed to testnet on 2025-01-04

[dev-dependencies]
# Add any test-specific dependencies here if needed
