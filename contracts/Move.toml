[package]
name = "sonar"
version = "0.1.0"
edition = "2024.beta"
license = "Apache-2.0"
authors = ["SONAR Protocol <team@sonar.xyz>"]

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/mainnet" }
# Explicitly including Walrus dependencies for SONAR marketplace
Walrus = { local = "./dependencies/walrus" }
WAL = { local = "./dependencies/wal" }

[addresses]
sui = "0x2"
sonar = "0x0"
walrus = "0xfdc88f7d7cf30afab2f82e8380d11ee8f70efb90e863d1de8616fae1bb09ea77"
# Note: Walrus Mainnet package address
# Deployed for Mainnet on 2025-03-27

[dev-dependencies]
# Add any test-specific dependencies here if needed
