# =============================================================================
# SEAL Key Server Environment Variables
# =============================================================================
# These are examples for documentation purposes.
# DO NOT commit actual values to git!
#
# For Railway deployment:
# 1. Go to Railway Dashboard → Your Project → Variables tab
# 2. Add these variables with real values (Railway encrypts them)
# 3. Never set these in railway.toml or commit them to git

# =============================================================================
# Required Variables
# =============================================================================

# Master seed for key derivation (64-character hex string)
# Generate with: cargo run --bin seal-cli gen-seed
#
# SECURITY: This is the root secret - if compromised, all derived keys are exposed
# Store in Railway secrets, never in git or logs
#
# Example format: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
MASTER_KEY=YOUR_64_CHAR_HEX_SEED_HERE

# Key server object ID from on-chain registration
# Get this by registering the key server on Sui testnet
#
# Steps to get this:
# 1. Generate derived public key (see README.md)
# 2. Call create_and_transfer_v1 on SEAL package
# 3. Copy object ID from transaction output
#
# Example format: 0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
KEY_SERVER_OBJECT_ID=0xYOUR_KEY_SERVER_OBJECT_ID_HERE

# Config file path (standard, usually no need to change)
CONFIG_PATH=/app/config/key-server-config.yaml

# =============================================================================
# Optional Variables
# =============================================================================

# Custom Sui RPC endpoint (defaults to public testnet RPC)
# Only needed if using a custom RPC node
# NODE_URL=https://fullnode.testnet.sui.io

# =============================================================================
# Deployment Checklist
# =============================================================================
#
# [ ] Generated master seed with seal-cli gen-seed
# [ ] Derived public key for index 0 (run key server locally once)
# [ ] Registered key server on-chain (got object ID)
# [ ] Set MASTER_KEY in Railway secrets
# [ ] Set KEY_SERVER_OBJECT_ID in Railway secrets
# [ ] Updated SONAR frontend .env with key server URL
# [ ] Tested /health endpoint after deployment
# [ ] Verified encryption flow in upload wizard

# =============================================================================
# Walrus Configuration
# =============================================================================
NEXT_PUBLIC_WALRUS_PUBLISHER_URL=https://publisher.walrus.space
NEXT_PUBLIC_WALRUS_AGGREGATOR_URL=https://aggregator.walrus.space
