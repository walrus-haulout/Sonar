{
  "$schema": "https://railway.app/json-schema/railway.json",
  "build": {
    "builder": "DOCKERFILE",
    "context": "seal-keyserver",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "variables": {
    "PORT": {
      "description": "HTTP port exposed by the key server",
      "value": "2024"
    },
    "MASTER_KEY": {
      "description": "SEAL master key (0x + 64 hex chars). Leave empty on first deploy to generate one.",
      "value": ""
    },
    "KEY_SERVER_OBJECT_ID": {
      "description": "Registered key server object ID (0x + 64 hex chars). Leave empty until after registration.",
      "value": ""
    }
  }
}
